# Contributor contract pointer

The canonical installed contract is
[`skills/thoughtloop/references/contracts.md`](../skills/thoughtloop/references/contracts.md).
Keep this root-level path as a contributor-facing pointer so repository tools
can find the contract without creating a second runtime schema.
