# Deep route

Use when framing, reversibility, or failure consequence justifies additional
independent challenge:

```text
task -> gapfinder -> discover/investigate -> decide -> execute
     -> verify -> review -> final-judgment
```

Review findings are evidence for final judgment, not a second unconsumed
verdict. A blocking finding must be resolved or reflected as `FAIL` or
`UNKNOWN`.
