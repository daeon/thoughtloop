# Deliberate route

Use when materially different approaches could change the result:

```text
task -> gapfinder -> discover -> decide -> execute -> verify -> final-judgment
```

Stop searching when one approach dominates the important constraints or a
focused experiment is more valuable than additional ideas.
