# Direct route

Use for a mechanical, tightly specified, reversible task:

```text
task -> execute -> verify -> final-judgment
```

Keep the route small, but still verify the stated acceptance criteria.
