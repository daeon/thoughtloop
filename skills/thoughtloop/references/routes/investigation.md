# Investigation route

Use for repository changes, debugging, logs, performance, migration,
compatibility, security, or release work:

```text
task -> gapfinder -> investigate -> decide -> execute -> verify -> final-judgment
```

Keep investigation read-only until a change is authorized. Measure before
performance edits and compare old and new behavior for compatibility claims.
