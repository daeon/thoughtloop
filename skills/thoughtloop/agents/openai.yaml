interface:
  display_name: "ThoughtLoop"
  short_description: "Discover, decide, execute, and independently prove"
  default_prompt: "Use ThoughtLoop for consequential coding work: explore when alternatives matter, execute economically, verify independently, and correct at the right depth."
policy:
  allow_implicit_invocation: true
